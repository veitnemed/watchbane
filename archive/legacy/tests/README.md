# Legacy tests (not maintained)

This directory is **not** collected by the active pytest suite (`testpaths = tests`).

`test.py` is a historical monolithic script that references removed Excel, KP/IMDb APIs, ML features, and `has_*` genre markup. Do not use it as a regression gate for current architecture.
